{
 "regions": ["sin1"],
  "buildCommand": "npm run build",
  "installCommand": "npm i --force --legacy-peer-deps",
  "outputDirectory": "./dist",
  "routes": [{
    "src": "./(.*)",
    "dest": "dist/main.js",
    "methods": ["POST", "GET", "PUT", "PATCH"]
  }]
}